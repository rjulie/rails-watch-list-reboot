<div class="container mt-5 card-container">
    <div class="col-8">
      <div class="card-form">
        <div>
          <%= simple_form_for(@list) do |f| %>
            <%= f.input :name %>
            <%= f.submit %>
          <% end %>

          <%= link_to "Back", lists_path %>
        </div>
      </div>
    </div>
</div>
